apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# -----------------------------------------------------------------
# Production overlay — aggregates all base layers.
# Point ArgoCD or `oc apply -k` at this directory.
# -----------------------------------------------------------------

resources:
 - enableMonitoring.yaml
 - serviceAccount.yaml
 - grafana-datasource.yaml
 - serviceMonitor.yaml
 - serviceMonitor.yaml
 - kueue-prometheus-rules.yaml
